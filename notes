Make sure the entire site is based locally on a timezone that can be specified in the settings. 

Add the ability to search by category on transactions page. Show by default time frames of the current month that can be edited as needed. Also add back the "View Bills"/"View Subs" button on the recurring page and it should take me back to the Transactions page filtered by the category matched to that bill